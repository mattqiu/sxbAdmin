$(document).ready(function () {    root_url = $("#root_url").html();    //修改单个商品上下架    $(".downUpSale").click(function () {        var stats = $(this).attr("value");        var goodsId = $(this).attr("name");        if (stats == 0)            var url = root_url + '/Goods/upSale';        else            var url = root_url + '/Goods/downSale';        $.ajax({            url: url,            data: {goods_id: goodsId},            complete: function () {                location.reload();            }        });    });});//是否通过审核function pass() {    if (!confirm("确定通过审核并向该用户下发邀请码？")) {        return false;    }}//商品列表提交操作验证function goodsManger() {    //检查要操作的商品id是否为空    var goodsId = getChkvalue();    var msg = '';    if (goodsId == false)        msg += '请选择要操作的商品';    else if ($("#batch option:selected").val() == false)        msg += '请选择要操作的选项';    else if ($("#category").css("display") == 'inline') {        if ($("#category option:selected").val() == false)            msg += '请选择分类';    }    else if ($("#shop").css("display") == 'inline') {        if ($("#shop option:selected").val() == false)            msg += '请选择店铺';    }    if (msg) {        alert(msg);        return false;    }}//checkbox全选 或不选function selAll(obj) {    var chk = document.getElementsByTagName("input");    var aa = [];    if (obj.className == 1) {        for (var i = 0; i < chk.length; i++) {            if (chk[i].type == 'checkbox') {                chk[i].checked = 'checked';            }        }        obj.className = 2;    }    else if (obj.className == 2) {        for (var i = 0; i < chk.length; i++) {            if (chk[i].type == 'checkbox') {                chk[i].checked = '';            }        }        obj.className = 1;    }}//获取已选中的checkbox的值function getChkvalue() {    var chk = document.getElementsByTagName("input");    var getValue = [];    for (var i = 0; i < chk.length; i++) {        if (chk[i].type == 'checkbox') {            if (chk[i].checked == true) {                if (!isNaN(chk[i].value)) {                    getValue.push(chk[i].value);                }            }        }    }    return getValue;}function batchDone(obj) {    var nowAct = obj.options[obj.selectedIndex].value;    /* var chkvalue = getChkvalue();     if(chkvalue == '')     {     alert("请选择要操作的选项!");     return false;     }     else     $("#sub").attr("disabled", false);     if( nowAct == false)     return false*/    if (nowAct == 'category') {        $("#category").css("display", 'inline');        $("#shop").css("display", 'none');    }    else if (nowAct == 'shop') {        $("#shop").css("display", 'inline');        $("#category").css("display", 'none');    }    else {        $("#shop").css("display", 'none');        $("#category").css("display", 'none');    }}/* *  获取 select元素的值 */function get_select(id) {    var aa = document.getElementById(id);    var val = aa.options[aa.selectedIndex].value;    return val;}function showRows(obj) {    var size = obj.options[obj.selectedIndex].value;    var url = $("#url").val();    var aa = $("#numpage").html();    var bb = aa.split("/");    var p = parseInt(bb[1]);    $.post(url, {p: p, row: size, is_ajax: 'yes'}, function (result) {        $("#listDiv").html(result.data);    }, 'json');}function ajax_page(obj, tourl) {    var url = root_url + "/" + tourl;    var size = get_select('size');    var aa = $("#numpage").html();    var bb = aa.split("/");    var countpage = parseInt(bb[0]);    var nowpage = parseInt(bb[1]);    var nowAct = obj.innerHTML;    var p = nowpage;    if (nowAct == '首页') {        p = 1;    }    else if (nowAct == '上一页') {        if (nowpage > 1) {            p--;        }        else {            p = 1;        }    }    else if (nowAct == '下一页') {        if (nowpage < countpage) {            p++;        }        else {            p = countpage;        }    }    else if (nowAct == '尾页') {        p = countpage;    }    else if (nowAct == '我跳') {        var jump1 = $("#jump").val();        var jump = parseInt(jump1);        if (( isNaN(jump)) || (jump == '')) {            alert("请输入数字");            return false;        }        p = jump;    }    $.post(url, {p: p, row: size, is_ajax: 'yes'}, function (result) {        $("#listDiv").html(result.data);    }, 'json');}